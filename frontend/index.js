import React from 'react';
import ReactDOM from 'react-dom';

const element = (
  <div>
    <h1>Hello, world!</h1>
    <p>This is my JSX code.</p>
  </div>
);

ReactDOM.render(element, document.getElementById('root'));
